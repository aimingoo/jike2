// 示例：拆分X中合并的三个名字

import X from "node:stream";

// 1）名字空间的别名
import Streams = X;

// 2）类型别名（interface X）
type Stream = X; // or X.Stream

// 3）变量引用（class X）
let Stream = X; // or X.Stream
