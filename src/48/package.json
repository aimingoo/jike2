{
  "name": "48",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "scripts": {
    "watch": "tsup src/index.ts --dts --format cjs --out-dir dist --sourcemap --watch",
    "build": "tsup src/index.ts --dts --minify --format cjs --out-dir dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "express": "^4.19.2",
    "msup": "^0.0.12",
    "tsup": "^8.1.0",
    "typescript": "^5.5.3"
  }
}
